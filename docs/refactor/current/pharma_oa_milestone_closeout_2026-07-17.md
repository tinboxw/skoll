# 医药 OA 里程碑关闭报告

> 批次：`pharma-oa-2026-07-04`
> 关闭日期：2026-07-17
> 正式进度源：`pharma_oa_work_items.md`

## 关闭结论

F6-F12 共 47 个正式 Work Item 已逐项完成独立验收。F12-05 复跑插件生命周期、医药业务端到端链路、性能与权限、全量后端、前端类型检查与生产构建后，未发现阻塞里程碑关闭的失败项。

本次关闭只更新 `docs/refactor/current/` 的正式治理证据，不修改 `old/`。后续实现不得继续向本批次追加 `Todo`；应由研发负责人根据本报告的后续建议，在 `current/` 创建新批次父任务表、Work Item 表和验收表。

## 完成度

| 里程碑 | 完成项 | 关闭证据 |
| --- | ---: | --- |
| F6 业务插件平台底座 | 8/8 | 主题、UI Kit、Host SDK、数据/API 契约、事件总线和生成器均已验收 |
| F7 工作流与动态表单 | 7/7 | 工作流领域/API/UI、表单设计、待办通知和审计回放均已验收 |
| F8 医药主数据 | 7/7 | 插件骨架、员工、药品、供应商、客户、仓库及导入导出均已验收 |
| F9 医药进销存 | 6/6 | 库存、采购、入库、销售出库、盘点调拨和预警闭环均已验收 |
| F10 协同与合规 | 8/8 | 公告、合同、资质、投诉、召回、冷链、合规看板和回归均已验收 |
| F11 CRM 与经营看板 | 6/6 | 客户跟进、销售机会、回款发票、指标、看板和报表导出均已验收 |
| F12 样板验收 | 5/5 | Demo、E2E、行业文档、性能权限和本关闭报告均已验收 |

## P0 验收总览

| 场景 | 结果 | 证据 |
| --- | --- | --- |
| 插件安装、启用、禁用、菜单、权限和审计 | 通过 | `scripts/smoke-pharma-oa-plugin.ps1` |
| 新员工与资质提醒 | 通过 | F12 demo seed 与 E2E 断言 |
| 采购申请与审批 | 通过 | 工作流完成并生成采购订单；后端 RBAC 生效 |
| 采购入库与批次库存 | 通过 | 批次、效期、余额和不可变流水一致 |
| 销售出库与资格拦截 | 通过 | 客户资格复核、库存扣减和流水一致 |
| 客户跟进 | 通过 | 计划记录与负责人归属正确 |
| 数据范围、审批权限和库存权限 | 通过 | JWT 身份、`401/403`、RBAC 和数据隔离测试通过 |
| 后端与 OpenAPI | 通过 | `go test ./...`、`go vet ./...` 和双份 OpenAPI 同步测试通过 |
| 前端工程质量 | 通过 | `npm run typecheck` 与 `npm run build` 通过 |
| 运行中服务 | 通过 | `http://127.0.0.1:8080/skoll/health` 与 `http://127.0.0.1:5173/skoll/workflow` 返回 200 |

## 问题清单

### 阻塞项

无。

### 开放项

| 下一批优先级 | 问题 | 当前边界 | 建议归属 |
| --- | --- | --- | --- |
| P0 | 非关键医药模块尚未全部接入后端细粒度路由权限映射 | F12-04 已覆盖本批次要求的审批、入出库、盘点、调拨及员工/客户关键路径；其余路径不能只依赖前端菜单权限 | 新权限路由注册批次 |
| P0 | 医药业务仓储仍以进程内实现为主 | 当前里程碑定位为可演示、可扩展样板，不构成生产持久化和容量承诺 | 新持久化批次 |
| P1 | JWT 暂无受信任的组织树声明 | 普通客户数据范围按本人负责人收紧，组织共享尚未开放 | 身份与数据范围批次 |
| P1 | 医药页面仍需全量中文默认、英文切换和可访问性复核 | 新增脚本与报告已中文默认并支持英文；历史页面存在未统一文案 | 前端质量批次 |
| P1 | 生产数据库查询与大数据量浏览器链路尚未形成容量基线 | 当前基准为 1000 条进程内服务样本 | 性能容量批次 |
| P2 | 前端构建存在 Sass legacy API 和第三方 Rollup 注释警告 | 构建成功，不影响本批次运行 | 依赖升级批次 |

## 后续任务建议

以下内容是下一批候选任务，不是本批次正式 `Todo`：

| 候选 ID | 建议任务 | 交付物 | 验收重点 |
| --- | --- | --- | --- |
| NEXT-01 | 插件路由权限自动注册 | manifest 路由到认证、RBAC、审计和 OpenAPI 的统一注册器 | 所有插件业务 API 默认认证；声明权限逐路由生效；拒绝有审计 |
| NEXT-02 | 医药业务持久化 | SQL 仓储、migration、seed、索引和事务边界 | MySQL/PostgreSQL/SQLite 契约测试；重启后数据保持；库存事务一致 |
| NEXT-03 | 组织数据范围声明 | JWT 组织声明、组织树策略和服务端范围解析 | 本人、组织、组织树和全量范围不可由请求参数扩大 |
| NEXT-04 | 医药 OA i18n 与无障碍验收 | 中文默认资源、英文资源、键盘/ARIA/焦点和响应式浏览器矩阵 | 所有页面状态和操作双语可用，无硬编码主流程文案 |
| NEXT-05 | 生产容量与稳定性基线 | 数据库压测、分页/索引报告、长时任务和浏览器大列表样本 | 明确 P95/P99、容量上限、慢查询和资源预算 |
| NEXT-06 | 发布准备 | 部署样例、数据备份恢复、升级演练、许可证与监管边界复核 | 可重复部署、恢复、升级和回滚；样板边界清晰 |

## 最终验证

```powershell
.\scripts\smoke-pharma-oa-plugin.ps1
.\scripts\smoke-pharma-oa-e2e.ps1
.\scripts\smoke-pharma-oa-performance-permission.ps1
go test ./...
go vet ./...

Set-Location web
npm run typecheck
npm run build
```

本关闭项无 API、权限 key、审计 action、migration、seed 或前端运行时代码变更。关闭后，`pharma_oa_task_board.md` 的 F6-F12 父任务与正式 Work Item 状态一致。
